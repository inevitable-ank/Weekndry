export const APP_NAME = 'Weekendly';

export const STORAGE_KEYS = {
  schedule: 'weekendly_schedule_v1',
  theme: 'weekendly_theme_v1',
} as const;

export const DEFAULT_CITY = 'Your city';

export const TIME_BLOCKS = ['morning', 'afternoon', 'evening'] as const;

export const DAYS = ['Saturday', 'Sunday'] as const;

export const ONE_MINUTE_MS = 60 * 1000;
export const ONE_HOUR_MS = 60 * ONE_MINUTE_MS;


